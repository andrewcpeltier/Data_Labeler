import { PageHostDirective } from './page-host.directive';

describe('PageHostDirective', () => {
  it('should create an instance', () => {
    const directive = new PageHostDirective();
    expect(directive).toBeTruthy();
  });
});
